apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kublai.fullname" . }}-config
  namespace: {{ include "kublai.namespace" . }}
  labels:
{{ include "kublai.labels" . | indent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
